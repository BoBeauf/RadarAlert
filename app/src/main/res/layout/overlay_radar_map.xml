<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="150dp"
    android:layout_height="210dp"
    android:background="@drawable/overlay_map_card_bg"
    android:clipToOutline="true">

    <!-- Carte osmdroid en fond (mode navigation) -->
    <org.osmdroid.views.MapView
        android:id="@+id/miniMap"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="false"/>

    <!-- Panneau vitesse style routier, haut gauche -->
    <TextView
        android:id="@+id/mapTvSpeed"
        android:layout_width="54dp"
        android:layout_height="54dp"
        android:layout_margin="6dp"
        android:gravity="center"
        android:textColor="#111111"
        android:textSize="22sp"
        android:textStyle="bold"
        android:background="@drawable/speed_sign_bg"
        android:elevation="6dp"/>

    <!-- Barre distance en bas -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:layout_gravity="bottom"
        android:background="#CC1565C0"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp">

        <TextView
            android:id="@+id/mapTvDistance"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:textColor="#FFFFFF"
            android:textSize="17sp"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/mapTvUserSpeed"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#CCE3F2FD"
            android:textSize="10sp"
            android:textStyle="bold"/>

    </LinearLayout>

    <!-- Badge multi-radars (haut droite) -->
    <TextView
        android:id="@+id/mapTvCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_margin="6dp"
        android:visibility="gone"
        android:textColor="#FFFFFF"
        android:textSize="10sp"
        android:textStyle="bold"
        android:background="@drawable/badge_count_bg"
        android:paddingStart="6dp"
        android:paddingEnd="6dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:elevation="6dp"/>

    <!-- Type caché (requis par le code) -->
    <TextView
        android:id="@+id/mapTvType"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"/>

</FrameLayout>
